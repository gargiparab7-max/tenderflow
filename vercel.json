{
    "installCommand": "cd f && npm install --legacy-peer-deps",
    "buildCommand": "cd f && npm run build",
    "outputDirectory": "f/build",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}