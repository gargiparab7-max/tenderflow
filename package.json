{
  "name": "tenderflow",
  "version": "1.0.0",
  "description": "TenderFlow - Tender Management System",
  "main": "electron.js",
  "scripts": {
    "dev": "node server.js",
    "backend": "cd backend && python -m uvicorn server:app --reload --host 127.0.0.1 --port 8000",
    "frontend": "cd frontend && npm run start",
    "start": "electron ."
  },
  "devDependencies": {
    "electron": "^40.6.1"
  }
}
